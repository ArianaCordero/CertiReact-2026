class Celda {
    fila: number;
    columna: number;
    valor: number;

    constructor(fila: number, columna: number, valor: number) {
        this.fila = fila;
        this.columna = columna;
        this.valor = valor;
    }
}